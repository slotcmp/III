# МАНИФЕСТ СЛОТ-КОМПОЗИТОРА (`slotCmp`)
## Декларация новой парадигмы реактивного TUI-моделирования
### Ревизия: #0819-QUANTUM-REACTIVE-DOD-MANIFEST
### Авторский контур: Экс-флешер & Школа профессора Е. М. Кудрявцева (GPSS)

---

## ВВЕДЕНИЕ
Настоящий документ фиксирует архитектурный суверенитет платформы `slotCmp` (бывш. `reflecmd` / `slotcmd`). Цель проекта — достижение предельной производительности рантайма V8 в средах ConPTY/TTY Windows за счет полного отказа от макроструктур современного веба и возврата к фундаментальным законам проектирования вычислительных систем.

Платформа оперирует концепцией **Квантового TUI-Движка**: локальные UHD-матрицы приборов находятся в суперпозиции до момента тактового "измерения" и дифференциального blit-переноса фазы растра на экран.

---

## ЧЕТЫРЕ СУВЕРЕННЫХ СМО-ЗАПРЕТА (0% САХАРА)

### 1. 0% OOP (Ноль процентов Объектно-Ориентированного Программирования)
* **Запрет**: Никаких классов (`class`), ключевых слов `new`, наследования (`extends`) и цепочек прототипов.
* **Физический DOD-смысл**: Исключение фрагментации кучи и деоптимизации скрытых классов (Hidden Classes) движка V8. Память аллоцируется строго мономорфными анемичными структурами данных через `Object.create(null)` и жестко запечатывается намертво с помощью `Object.preventExtensions()`.

### 2. 0% try/catch (Полное исключение перехвата исключений в логическом слое)
* **Запрет**: Контур прикладной редукции данных и тактового продвижения шины не имеет права содержать блоки `try/catch`. 
* **Физический DOD-смысл**: Блок `try/catch` деоптимизирует компилятор TurboFan, создавая ломаные нелинейные ветвления и просаживая частоту кадров. Все гварды и санитария данных (например, отсечение `NaN` и Out-of-Bounds индексов) осуществляются превентивно и строго инлайново `if (isNaN(...))`. Исключение составляют лишь изолированные дисковые воркеры ввода-вывода.

### 3. 0% RegExp (Абсолютный запрет на Регулярные Выражения)
* **Запрет**: Поиск, векторизация флекс-процентов из `layout.json` и разбор байт выполняются без методов `.match()`, `.test()` или регулярных выражений.
* **Физический DOD-смысл**: Регулярные выражения вызывают недетерминированные аллокации строк и нагружают процессор скрытыми конечными автоматами. Анализ выполняется посимвольно/побайтово через прямые логические вентили и числовые ASCII/управляющие коды (`byte === 27`, `.charCodeAt(0)`).

### 4. 0% Polling / 100% Reactive Interrupts (Ликвидация холостого хода)
* **Запрет**: Отказ от бесконечных циклов `setInterval` для опроса клавиатуры, мыши или воркеров.
* **Физический DOD-смысл**: Шина СМО (`bus.js`) является пассивным реактивным планировщиком (Event-Driven Scheduler). Ядро просыпается строго в момент прихода физического аппаратного прерывания от абстрактных приборов и засыпает, потребляя 0% CPU вне импульсов.

---

## ТРИ ФАЗЫ ГИБРИДНОГО СЛОТ-КОМПОЗИТОРА

Каждый квант времени `ADVANCE` распределяется по шине на базе строгого приоритета аппаратных прерываний, аналогичного контроллеру прерываний CPU (APIC):

1. **Фаза А: Реактивный захват прерывания (`TRIGGER_RESIZE` / `KEY_PRESSED`)**  
   Абстрактный прибор моментально принимает транзакт прерывания любой природы в локальную FIFO-очередь `localQueue` через блок `SEIZE` и гасит такт, не выполняя инлайновых расчетов в основном потоке.
   
2. **Фаза Б: Квантование и Асинхронный расчет**  
   Фоновые изолированные потоки операционной системы (`layout_worker.js`, `vfs_worker.js`, `animation_worker.js`) осуществляют тяжелую вычислительную работу (3-проходной расчет флексов, Tween-интерполяцию линеек, чтение дисков Windows) независимо от ядра хоста. По завершении они выстреливают IPC-команду обратной связи (`INJECT_GEO_MAP` / `INJECT_VFS_DATA`).
   
3. **Фаза В: Приоритетное "Измерение" и Двойная Буферизация кадра**  
   Шина СМО просыпается по импульсу и запускает детерминированный арбитраж: сначала разгребаются высокоприоритетные IRQ координат окон, и только в самом конце — замыкающий такт кадровой развёртки (Канал 1 / `render_unit.js`). Низкоуровневый `flusher.js` берет Back-буфер растра, попиксельно сверяет его со старой фазой теневого Front-зеркала, вычисляет **"Грязные Пиксели" (Dirty Cells)** и одним байтовым залпом `fs.writeSync` накрывает изображение в терминале без мерцания и без вызова деструктивной очистки `\x1b[2J`.

---

## ПРЕЕМСТВЕННОСТЬ СИСТЕМЫ
Слот-Композитор доказывает: строгое имитационное моделирование СМО в канонических терминах `SEIZE -> ADVANCE -> RELEASE -> DEPART` применимо не только к абстрактному анализу заводов, но и является высшей формой оптимизации архитектуры современных программных комплексов. 

Декларация утверждена рантаймом. Направление векторов зафиксировано в DNS на `slotcmp.ru`.


## ИСТОРИЧЕСКИЙ СЛЕД И ПРЕЕМСТВЕННОСТЬ (ИЗ СТАРЕНЬКОГО)

### 1. Философия Сдвига Фазы: От Flash/ActionScript к ConPTY
Современный веб погряз в абстракциях DOM-деревьев, виртуальных слоев и реактивного мусора, который бесконечно аллоцирует память. Мы возвращаем в `slotCmp` истинную религию **экс-флешеров**:
* **Экран как Растр**: Интерфейс — это не дерево объектов, это плоская UHD-матрица фиксированной сетки знакомест.
* **Байтовый Blit-перенос (BitBlt)**: Вместо хаотичного обновления элементов мы, как в старом добром Macromedia Flash, готовим кадр целиком «вслепую» в Back-буфере ОЗУ (двойная буферизация). 
* **Покадровый Тайминг**: Сдвиг каретки вывода и наложение рамок происходят атомарно, попиксельно, за один системный такт. Если пиксель не изменил свою фазу (цвет или символ) — он признается статичным. Трафик в stdout равен нулю. Мы не перерисовываем интерфейс, мы управляем интерференцией его дельты.

### 2. Академический Контур: Корни GPSS World и Советской Школы СМО
Архитектура абстрактных приборов `slotCmp` напрямую наследует математический аппарат, заложенный профессором Е. М. Кудрявцевым. Мы переносим симуляцию физических потоков на обслуживание интерфейсных прерываний ОС Windows:
* **Транзакт как Волна**: Любое событие (клик мыши SGR, байт из stdin, чанк от VFS) — это не просто колбэк, это физический транзакт, имеющий параметры-приоритеты `P1, P2, P3`.
* **Аппаратные Блоки Переключения**: Конструкции `SEIZE -> ADVANCE -> RELEASE -> DEPART` — это прямой цифровой аналог релейных схем и логических вентилей первых ЭВМ. Прибор полностью изолирован: он не знает о существовании соседа, он лишь квантует время, пропуская через себя поток транзактов.
* **Взаимное Грязнение (Каскадная Инвалидация)**: Один прибор не имеет права мутировать память другого. Но он может сгенерировать транзакт-прерывание высокого приоритета. На следующем шаге СМО контроллер прерываний (APIC) разбудит связанный прибор, заставив его осознанно изменить свой внутренний стейт. Это чистая математика связанных волновых потоков Кендалла.

---



